# Travel Gallery Images

Place real travel photos in this folder and update `data/portfolio.ts` so each `src`
points to the new file path, for example:

```ts
src: "/images/travel/my-real-photo.jpg"
```

The current SVG files are original placeholder artwork for layout and hover states.
Keep image names stable if you want to replace a placeholder without touching code.
